anthropic>=0.40.0
python-docx>=1.1.0
python-pptx>=1.0.0
openpyxl>=3.1.0
PyYAML>=6.0
requests>=2.31.0
# React frontend backend (server.py) — the only UI. The Streamlit app it replaced
# (app.py) was a one-shot generator and has been removed along with that mode.
fastapi>=0.110.0
uvicorn>=0.29.0
# Google Sign-In (OAuth) — verify ID tokens server-side
google-auth>=2.28.0
# Turso / libSQL cloud DB (only used when TURSO_DATABASE_URL is set; free host + persistent data)
libsql-experimental>=0.0.37
